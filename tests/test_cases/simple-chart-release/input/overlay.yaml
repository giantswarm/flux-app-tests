apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hello-world
spec:
  chart:
    spec:
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: giantswarm
        namespace: default
